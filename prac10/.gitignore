# Terraform 로컬 파일 (Remote Backend 사용 시 불필요)
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*

# 민감 정보 (tfvars는 제외, example 파일은 허용)
*.tfvars
!*.tfvars.example

# 운영체제
.DS_Store
